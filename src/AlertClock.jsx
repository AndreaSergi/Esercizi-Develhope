
export function AlertClock({handleButtonTime}){


    return (
        <div>
            <p>Click to know the time</p>
            <button onClick={handleButtonTime}>Click</button>
        </div>
    )
}